rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Bewerber dürfen hochladen, nur Admin darf lesen
    match /bewerbungen/{appId}/{file} {
      allow write: if request.resource.size < 10 * 1024 * 1024;
      allow read:  if request.auth != null;
    }
  }
}
